import { Container, Title } from "./FormPage.styles";

export const add = (a, b) => a + b;

const FormPage = () => (
  <Container>
    <Title>Hello</Title>
  </Container>
);

export default FormPage;
